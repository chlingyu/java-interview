# 八股文面试题 · 统一规范

本文件是本仓库面向 **Codex** 和 **Claude** 的统一规则入口。

- `八股文面试题/` 目录下所有 `README.md` 与 `导出/*.md` 的写作、改写、补充、审查，全部以本文件为准
- 如其他临时提示、旧文档、个人习惯与本文件冲突，以本文件为准
- 审查时按本文件找问题，不按个人偏好打分
- **协作项目**：如涉及八股文写题/审查协作，必须先读 `ai_collab.md`（协作协议 + 进度记录）和 `content_gap_analysis.md`（缺口清单）

## 参考资源（每次写八股文时综合以下资源的最佳回答）

| 优先级 | 资源名称 | 链接 | 擅长领域 |
|--------|---------|------|---------|
| ⭐ 必查 | 二哥·面渣逆袭 | javabetter.cn | Java基础/并发/JVM/Spring，手绘图解最通俗 |
| ⭐ 必查 | 小林 coding | xiaolincoding.com | MySQL/Redis/网络/操作系统，图解原理最强 |
| ⭐ 必查 | JavaGuide | javaguide.cn | 全技术栈，体系最全 |
| 可查 | 面试鸭 | mianshiya.com | 全技术栈刷题，大厂真题多 |
| 可查 | CS-Notes | github.com/CyC2018/CS-Notes | 计算机基础（网络/OS/算法） |
| 可查 | bugstack 虫洞栈 | bugstack.cn | Java底层/源码/设计模式 |
| 可查 | 牛客网 | nowcoder.com | 真实面经、追问场景 |

**写题流程**：搜 ⭐必查 资源 → 综合各家最佳答案 → 按下方模板输出

**资源冲突处理**：当不同资源说法不一致时，优先级为：官方文档/源码 ＞ 多个资源一致的说法 ＞ 单个资源的独家说法

## 每章题量规划

> [!IMPORTANT]
> 此表为 **完整技术栈清单**，开始写之前必须确认没有遗漏。如有新增技术栈需求，先更新此表。

| 技术栈 | 建议题数 | 核心考点 |
|--------|---------|----------|
| Java 基础 | 20~25 | 面向对象/String/异常/泛型/反射 |
| Java 集合 | 12~15 | HashMap/ArrayList/ConcurrentHashMap |
| Java 并发 | 15~20 | 线程池/synchronized/volatile/AQS/ThreadLocal/CompletableFuture |
| JVM | 12~15 | 内存结构/GC 算法与收集器/类加载/对象头/调优 |
| Spring | 12~15 | IoC/AOP/Bean 生命周期/事务/MVC/Filter vs Interceptor |
| MySQL | 15~20 | 索引/事务ACID/MVCC/锁/三大日志/SQL优化 |
| Redis | 10~15 | 数据类型/持久化/缓存穿透击穿雪崩/分布式锁/集群 |
| 计算机网络 | 8~12 | TCP 三次握手四次挥手/HTTP/HTTPS/状态码 |
| 操作系统 | 6~10 | 进程线程/死锁/虚拟内存/用户态内核态 |
| 设计模式 | 6~8 | 单例/工厂/代理/策略/观察者/模板方法 |
| MyBatis | 5~8 | #{} vs ${}/缓存/动态SQL/Mapper绑定 |
| 消息队列 | 7~10 | 为什么用MQ/消息不丢失不重复/顺序性/死信队列 |
| 分布式与微服务 | 15~18 | CAP/分布式事务/Gateway/OpenFeign/Nacos/Sentinel/负载均衡 |

## 验证标记

每次回答用户时，必须在回答的**开头**加上「大哥」，以确认本规范已生效。

## 执行纪律

写八股文题目时，**禁止做以下无意义的前置工作**：
- ❌ 不要先建 task.md 任务追踪文件
- ❌ 不要反复调用 task_boundary 更新状态
- ❌ 不要先写 implementation_plan.md 再等审批

**正确流程：读规范 → 技术栈完整性检查 → 直接写内容 → 写完后按自查清单核对 → 通俗化复核 → 题量充分性验证 → 交付。** 一步到位，不要空转。

## 技术栈完整性检查（强制前置）

开始写**任何新内容**之前，必须：

1. 对照上方「每章题量规划」表，确认所有技术栈都已列入计划
2. 如果用户要求写全部技术栈，必须 **web search** 搜一下「Java后端面试八股文 完整技术栈」确认没有遗漏
3. 对于每个技术栈，检查子主题是否覆盖完整（如「分布式与微服务」不能只有理论没有 Spring Cloud 组件）

> [!CAUTION]
> 把多个细分领域合并到一个技术栈时（如 Spring Cloud 放在「分布式与微服务」里），必须确保每个子领域都有足够题量，不能只写理论不写组件。

## 分批写入（强制）

每次工具调用**最多写 4 道题**。禁止一次性生成整个文件的所有题目回答。

- **原因**：一次写太多题会导致 AI 端 thinking 时间过长，叠加网络不稳定极易超时断连，前功尽弃
- **执行规则**：
  1. 按章节或 4 题为一批，用一次写入工具写入
  2. 写完一批后立即写下一批，**不要等用户确认**
  3. 最后一批写完后再做自查清单
- **示例**：12 道题拆成 3 批（1-4、5-8、9-12），每批一次工具调用

## 写作原则

1. **要点到位，不废话**：直接给结论，不绕弯子
2. **说人话**：用口语化表达，禁止学术腔（详见「通俗化复核」章节）
3. **适合背诵**：提供口诀、规律、一句话总结
4. **讲清楚原理**：涉及底层时要具体到存了什么值、怎么存的，不能含糊
5. **适合基础差的**：每一步都要解释清楚，不跳步骤、不预设读者知道

## 回答结构模板

每道题的回答应包含以下部分（按需取舍，不是每题都要全部用上）：

### 必选项

- **开头一句话总结**：先给结论，让读者一秒抓住重点
- **要点列表或表格**：对比类信息用表格，罗列类信息用列表

### 可选项（按需使用）

- **背诵口诀**：帮助记忆的规律、助记短语
- **代码示例**：关键点用代码说话，但不堆代码，每段不超过 10 行
- **面试话术**：用引用块 `>` 给出「面试时怎么说」的模板。语气用**半正式口语**，像跟面试官聊天，不要像背书
- **面试挖坑提醒**：标注常见误区（如「这题面试经常挖坑」），主动帮读者避坑
- **关联知识点**：顺带提一下紧密相关的概念（如讲基本类型时顺带提包装类型），但不展开太多
- **常见坑点 / 追问**：面试官可能追问的点

## 格式规范

- 章节编号连续：一、二、三……
- 题号全局连续：1、2、3……不跳号
- 加粗 `**` 用于关键术语和结论
- 行内代码 `` ` `` 用于类名、方法名、关键字
- 引用块 `>` 用于面试话术建议
- 表格用于对比类信息
- 每道题的正文控制在 **30 行以内**（不含代码块和表格），不写长篇大论

## 细节要求

- 说到底层存储时，要说清楚**存的是什么值**（如 boolean 的 true 存为 1，false 存为 0）
- 说到「用 xxx 实现」时，要说清楚**具体怎么用的**
- 不要只说「性能更好」，要说**为什么更好、好在哪里**
- 涉及版本差异时（如 JDK 1.7 vs 1.8），要明确标注版本
- 涉及具体实现时，要标明**是哪个实现**（如 HotSpot JVM、InnoDB 引擎、Redis 6.0 等），不要笼统地说「JVM 中」「数据库中」
- 以上规则适用于所有话题，不限于 Java 语言本身，MySQL、Redis、Spring、网络等话题同样遵守

## 顺序约束

题目和章节的排列必须遵循 **「概念先介绍，再引用」** 原则：

- 基础概念的章节排在使用这些概念的章节之前
- 底层原理排在依赖它的上层机制之前
- 对比类题目排在被对比的各项内容之后（先了解，再比较）
- 具体规则示例：
  - 基本数据类型 → 再讲 `==` 对基本类型和引用类型的区别
  - JMM、CAS → 再讲 synchronized、volatile
  - 存储引擎（InnoDB vs MyISAM）→ 再讲聚簇索引（InnoDB 特有）
  - IOC → Bean 生命周期 → Bean 作用域 → 循环依赖（同一条知识线不要被其他章节割断）
  - 先分别介绍 Kafka、RabbitMQ → 再做对比

## 跨章节重复概念处理

同一个概念可能在多个技术栈中出现（如 hashCode 在 Java 基础和 Java 集合都会提到），处理规则：

- **第一次出现**：完整讲解
- **第二次出现**：一句话带过 + 标注「详见 XX 章第 X 题」，不重复展开

## 文件结构

每个 `README.md` 的结构：

```md
# 章节标题

---

## 一、分类名
### 1. 题目？
（回答内容）

### 2. 题目？
（回答内容）

## 复习优先级（3~5 年）
| 优先级 | 题目 |
|--------|------|
| P0 | 高频必背 |
| P1 | 中频建议掌握 |
| P2 | 低频了解即可 |
```

## 生成后自查清单（强制执行）

每次写完题目后，**必须逐条核对**以下 7 项，不通过的必须补充后再交付。不允许跳过此步骤。

| # | 检查项 | 要求 | 反例 |
|---|--------|------|------|
| 1 | **具体到值/实现** | 涉及底层存储时，具体到存了什么值、用什么结构 | ❌「boolean 占 4 字节」→ ✅「HotSpot 中单个 boolean 底层用 int 存，true 存为 1，false 存为 0，占 4 字节」 |
| 2 | **区分场景** | 不同场景下行为不同时，必须分开说明 | ❌ 只说 `==` 比地址 → ✅ 分字面量、new、intern() 三种场景说明 |
| 3 | **面试挖坑提醒** | 有常见误区的题必须标注「这题面试经常挖坑」 | ❌ 平淡叙述 → ✅ 主动提示易踩坑的点 |
| 4 | **背诵口诀** | 对比类、罗列类题目必须提供口诀或助记短语 | ❌ 没有口诀 → ✅「重载看参数，重写看父子」 |
| 5 | **版本差异标注** | 涉及 JDK 版本差异时，必须标明具体版本号 | ❌「常量池在堆中」→ ✅「JDK 7 之前在永久代，JDK 7+ 移到堆中」 |
| 6 | **关联知识自然引出** | 紧密相关的概念要顺带提及（不展开太多） | ❌ 讲 String 不提常量池位置 → ✅ 顺带一张版本对比表 |
| 7 | **高频追问覆盖** | 面试官常见追问必须覆盖到 | ❌ 讲泛型不提通配符 → ✅ 补充 extends/super 和 PECS |

**自查流程：**
1. 写完所有题目后，逐题过一遍上面 7 项
2. 用表格列出每题的通过/不通过情况
3. 不通过的项直接补充，不要问用户「要不要补」

## 通俗化复核（强制执行）

自查清单通过后，必须再做一轮**通俗化复核**，确保基础差的读者能看懂每一句话。

### 复核思路

逐题通读，对每一句问自己：「一个没学过这个概念的人，能看懂这句话吗？」

### 8 项复核规则

| # | 检查项 | 要求 | 反例 → 正例 |
|---|--------|------|----------|
| 1 | **数学符号改人话** | 禁止出现 `⊃`、`O(1)`、`2^31` 等数学符号，必须翻译成人话 | ❌ `O(1)` → ✅ 「像查字典先查首字母，一步定位」 |
| 2 | **英文缩写加中文** | 首次出现的英文缩写必须括号注释中文 | ❌ `RPC 调用` → ✅ 「远程方法调用（RPC）」 |
| 3 | **专业术语简化** | 可以用大白话说清的，不要用专业术语 | ❌ 「协变返回」 → ✅ 「如父类返回 Number，子类可返回 Integer」 |
| 4 | **抽象描述加例子** | 说到抽象概念时，必须紧跟一个具体例子 | ❌ 「统一接口不同实现」 → ✅ 「同一个方法在不同对象上表现不同」 |
| 5 | **表格数据利落** | 表格中的数字/范围要用读者能感知的单位 | ❌ `-32768 ~ 32767` → ✅ 「-3万 ~ 3万」 |
| 6 | **消除歧义** | 一句话可能有两种理解时，必须改写消除歧义 | ❌ 「编译报错」(读者会想“报什么错”) → ✅ 「不能用（基本类型不是对象，没有方法）」 |
| 7 | **类比辅助理解** | 抽象机制用生活类比辅助理解 | ❌ 「定位到哪个桶」 → ✅ 「像查字典先查首字母」 |
| 8 | **内部引用不断裂** | 提到其他题目的概念时，确保该概念已在前面的题目中介绍过 | ❌ 第 5 题突然提「常量池」但前面没解释 → ✅ 在第 6 题 String 不可变中先解释常量池 |

### 复核流程

1. 自查清单 7 项通过后，全篇通读一遍
2. 逐句过上面 8 项规则
3. 有问题的直接改，不要问用户
4. 改完后用表格向用户汇报修改了哪些地方

## 题量充分性验证（强制后置）

通俗化复核通过后，必须执行最终的**题量充分性验证**，确保每个技术栈的题目覆盖了主流面试资源的高频考点。

### 验证流程

1. **web search** 搜索「{技术栈名} 面试必问 高频题 2025 javaguide」
2. 对比搜索结果中的高频题清单和已写题目，列出差异
3. 差异中属于高频/中频的题目必须补上
4. 用表格向用户汇报验证结果（哪些通过、哪些需要补充）

### 验证标准

| 情况 | 处理 |
|------|------|
| 已有题量 ≥ 建议题数下限 且 高频考点全覆盖 | ✅ 通过 |
| 已有题量 < 建议题数下限 | ❌ 必须补题 |
| 题量达标但缺少某个高频考点 | ❌ 必须补题 |
| 子领域题量严重失衡（如分布式理论4题但Spring Cloud组件仅1题） | ❌ 必须补题 |

> [!WARNING]
> 不要因为总题量达标就跳过验证。一个技术栈内部的子领域分布不均（如只写了理论没写实践组件）同样是严重问题。
