# CLAUDE.md - 项目约束

## 八股文面试题写作风格规范（B+C 混合模式）

### 目标读者
基础较弱的 Java 开发者，准备 3~5 年经验的后端面试。假设读者只有"能写 CRUD"的水平，对底层原理了解很少。

### 写作模式：B+C 混合

根据题目的面试高频程度，使用不同深度的写作风格：

#### ⭐⭐⭐⭐~⭐⭐⭐⭐⭐ 高频题 → C 风格（通俗故事型）

1. **用生活比喻解释原理**：先用一个通俗场景让读者理解概念，再过渡到技术细节。比喻要有**完整的小故事**（至少 3~4 句话的场景描述），不要只用一句比喻就跳到技术
2. **比喻和技术之间要有充分的过渡段（"回到技术"段）**：比喻讲完后，不要直接甩表格/代码。用 **3~5 句话**把比喻中的**每个角色**逐一"翻译"成技术概念，让读者带着理解往下读。过渡段必须覆盖比喻中出现的所有关键类比对象，不能只翻译一半就进入代码。
   - ✅ 好的过渡："上面说的'格子'就是 HashMap 的数组槽位（bucket），'手机尾号'就是 hashCode 对数组长度取模的结果，'核对身份证'就是调用 equals() 方法比较 key。所以整个取快递的过程，就是 HashMap 先算 hashCode 定位桶，再用 equals 确认 key，两步缺一不可。"
   - ❌ 坏的过渡："上面说的就是 HashMap 的原理。" ← 一句话带过，等于没过渡
3. **讲完"是什么"还要讲"为什么"（追问层）**：每道高频题不能只讲结论和用法，还要用通俗话解释**底层原因**。读者最困惑的不是"怎么用"，而是"为什么是这样设计的"。如果涉及 JVM 内存、编译器行为、设计考量等，用一段"**为什么这样设计？**"或在原理详解中穿插解释。
   - ✅ 好的："为什么 == 比较的是地址？因为引用变量里存的就是对象在堆内存中的地址值，== 对引用类型本质上是在比这个地址数字是否一样。"
   - ❌ 坏的："== 比较地址，equals 比较内容。" ← 只说了结论，没解释原因
4. **专业术语首次出现时标注**：用 **加粗（一句话解释）** 格式。括号里的解释必须是人话，不能用另一个术语解释术语。
   - ✅ 好的：**STW（Stop The World，所有业务线程暂停，就像全公司停工等保洁打扫完）**
   - ❌ 坏的：**栈帧（局部变量表、操作数栈、动态链接、返回地址）** ← 四个生词解释一个生词，等于没解释
5. **关键数字用 ⚡ 标注**：所有阈值、默认值必须写死，不要模糊表述。例如：链表长度 ⚡**≥ 8** 且数组长度 ⚡**≥ 64** → 转红黑树
6. **信息密度控制**：一个知识点内，表格行数不超过 5~6 行。如果对比项超过 6 个，拆成"核心对比"和"补充了解"两部分，核心部分重点讲 2~3 个最常考的，其余简要带过
7. **复杂流程用分步骤 + 示意图**：拆成带编号的步骤，每步用一句通俗话概括"这步在干嘛"，再跟技术细节。涉及以下内容时**必须配图**：
   - 内存布局（堆/栈/方法区的关系）→ 用 ASCII 图或 Mermaid
   - 流程/生命周期（对象创建、方法调用链）→ 用 Mermaid flowchart
   - 层级/继承关系 → 用 Mermaid classDiagram 或 graph
   - 前后对比（编译前 vs 编译后、浅拷贝 vs 深拷贝）→ 用 ASCII 图并排展示
8. **每题附"🎤 面试这样答"**：给出一段可以直接参考的面试回答话术（⚡ **控制在 80~150 字**，太短没料，太长记不住）
9. **配图要求**：优先使用 Mermaid 语法画流程图/结构图（GitHub 原生渲染），复杂数据结构可用 ASCII 图。避免纯文字描述空间关系。**每道高频题至少要有一个图**（Mermaid 或 ASCII），禁止全文纯文字+表格

#### ⭐⭐~⭐⭐⭐ 普通题 → B 风格（结构化讲解型）

1. **一句话回答** + 关键原理简述
2. **必须有通俗过渡（"说人话"段）**：不需要完整比喻故事，但 **必须** 有 2~3 句话用大白话解释清楚这个东西到底是干嘛的、日常开发中什么时候会碰到。不能上来就甩术语表格或代码。
   - ✅ 好的："简单说，throw 就像你在代码里主动'摔杯子'——发现不对就抛个异常出去。throws 是在方法签名上贴个'警示牌'告诉调用者：'我这个方法可能会出问题，你得有心理准备'。"
   - ❌ 坏的："throw 抛出异常对象，throws 声明异常类型。"（← 直接甩定义，跟查字典没区别）
3. 保留专业术语标注（同 C 风格的标注规则，术语解释必须是人话）
4. 核心代码示例（带中文注释）
5. 用表格对比差异，简洁明了
6. 不需要完整比喻故事和"🎤 面试这样答"
7. **篇幅要求**：普通题总篇幅控制在 **30~60 行**（Markdown 行数），不能写得比高频题还长，也不能短到只有一个表格

### 每道题的结构模板

#### 高频题（C 风格）模板

```markdown
### N. 面试题目？

> ⭐⭐⭐⭐⭐ 必考 | 难度：⭐⭐⭐

**一句话回答**：xxxxx

**通俗理解**：（用比喻/生活场景解释原理，至少 3~4 句话的完整小故事）

**回到技术**：（3~5 句过渡，把比喻中的每个角色逐一"翻译"成技术概念）

**原理详解**：
（分步骤讲解 + 关键数字 ⚡ 标注）
（必须包含至少一个 Mermaid 或 ASCII 示意图）
（如果有"为什么这样设计"的追问点，在这里加入解释）

**代码示例**：（核心代码，带中文注释）

**🎤 面试这样答**：
> "xxxxxx"（80~150 字，可以直接背的面试话术）
```

#### 普通题（B 风格）模板

```markdown
### N. 面试题目？

> ⭐⭐ 了解 | 难度：⭐⭐

**回答**：一句话回答 + 2~3 句通俗大白话解释（必须说人话，不能只甩定义）。
然后再展开讲解原理，专业术语加粗标注。

**代码/表格**：（如需要）
```

### 章节结构

每章 README.md 的固定结构：

```markdown
# XX - 主题名

> 一句话说明这章覆盖什么。

---

## 一、板块名
### 1. 题目
...

## 面试高频程度排序（3~5年）
| 优先级 | 题目 |
|--------|------|
| ⭐⭐⭐⭐⭐ | xxx |
| ...    | ...  |
```

### 注意事项

- **不要写干巴巴的纯表格堆砌**，高频题必须有温度
- **不要省略关键数字**，所有阈值、默认值都要写清楚
- **专业术语不能落下**，但第一次出现必须用通俗语言解释，不能用术语解释术语
- **代码示例要有中文注释**，不要写纯英文注释
- **篇幅控制**：高频题可以长（100~180 行/题），普通题要精简（30~60 行/题），每章控制在合理长度内
- **比喻到技术不能断层**：写完比喻后必须有 3~5 句的过渡段，把比喻中的**每个**关键类比对象映射回技术概念，再进入详解
- **不能只说"是什么"，要说清"为什么"**：高频题必须包含底层原因的解释（JVM 怎么处理、编译器做了什么、设计者为什么这样选择），用通俗话讲，不要留下"知道结论但不理解原因"的空白
- **表格不能当万能工具**：超过 6 行的对比表格要拆分，核心的重点讲、次要的简要带过
- **B 风格也要有人味**：即使是低频题，也必须有 2~3 句通俗解释，不能直接甩术语+表格。写完检查：如果把通俗过渡删掉剩下的内容跟百度百科/官方文档没区别，说明人味不够
- **每道高频题至少配一个图**：Mermaid 流程图、类图、ASCII 内存布局图都可以。纯文字+表格的高频题视为不合格
- **B 风格必须极简**：普通题（B风格）**坚决不能**有完整长篇比喻和`🎤 面试这样答`模块。只能保留"2-3句大白话解释"。如果普通题写得跟高频题一样长，视为格式越界不合格
- **Mermaid 绘图避坑**：在绘制 Mermaid 流程图（graph）时，节点文本中如果包含括号 `()` 或特殊符号，**必须用引号 `""` 将节点文本整个包裹起来**（如 `A["调用方法()"]`），否则极易导致渲染引擎报错崩溃
- **体感自检**：写完一道题后，想象一个只会写 CRUD 的初级开发读完后能不能给别人讲明白。如果不能，说明要么比喻不够直白，要么过渡不够充分，要么缺少"为什么"的解释
