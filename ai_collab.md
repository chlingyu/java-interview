# AI 协作协议 · Antigravity × GPT

> **⚡ 快速恢复**：无论是 Claude 还是 GPT，新对话读到本文件即可恢复上下文。
> 1. **先读** `AGENTS.md`（写作规范）→ 本文件（协作协议 + 历史进度）→ `content_gap_analysis.md`（缺口清单）
> 2. **角色确认**：Claude = **Writer**（写题），GPT = **Reviewer**（审查）
> 3. **当前进度**：Round 1-10 已完成，Round 11 已写完待审（分布式与微服务 P2 实战组件：限流算法/Seata AT/TCC/接口幂等性），P1 缺口全部补完，P2 进行中
> 4. **P1 建议顺序**：~~① HTTPS TLS 握手（计算机网络）~~ ~~② 线程池核心线程数（Java 并发）~~ ~~③ 零拷贝（操作系统）~~ ~~④ 联合索引底层 B+ 树（MySQL）~~ ~~⑤ RocketMQ 事务消息（消息队列）~~ → **全部完成！**
> 5. **用户只需说「继续八股文」**，AI 读完本文件就知道该干什么

> 本文件定义两个 AI 助手（Antigravity / GPT）在本仓库中的协作规则。  
> **用户是唯一的路由器和裁判**，AI 之间不直接通讯，所有交互通过本文件 + 各模块 README.md 间接完成。

> [!IMPORTANT]
> 如本文件与 `AGENTS.md` 冲突，以 `AGENTS.md` 为准；如与具体轮次记录冲突，以**用户最新明确指令**为准。

---

## 一、角色分工（固定不换）

| 角色 | 担任者 | 职责 | 操作范围 |
|------|--------|------|---------|
| **Writer（写手）** | Antigravity | 补题、改题、格式调整 | 直接修改 `八股文面试题/*/README.md`，并同步对应 `导出/*.md` |
| **Reviewer（审查员）** | GPT | 按 `AGENTS.md` 审查质量、指出事实错误、措辞问题 | 只在本文件的审查区写意见，**不直接改 README** |
| **Router（路由器/裁判）** | 用户（你） | 发起任务、传递审查意见、拍板定稿 | 全部文件 |

> [!IMPORTANT]
> **GPT 不改代码，Antigravity 不自我审查。** 如果需要反过来（GPT 写、Antigravity 审），在任务头里显式声明。

---

## 二、工作流程（每轮固定 5 步）

```
步骤 ①  用户 → Antigravity：发补题/改题指令
步骤 ②  Antigravity：直接写题；完成后在下方「工作记录」区填写本轮摘要
步骤 ③  用户 → GPT：「读 ai_collab.md 的最新一轮 + 对应 README.md + 对应导出文件 + AGENTS.md，做审查」
步骤 ④  GPT：给出结构化审查意见，用户贴回本文件的审查区
步骤 ⑤  用户 → Antigravity：「读 GPT 审查意见，做修改」或「审查通过，进入下一轮」
```

### 关键规则

1. **一轮只处理一个模块**（如 JVM / MySQL），不跨模块混写
2. **每轮最多 4 道题**（遵守 AGENTS.md 分批写入规则）
3. **审查意见必须结构化**（见下方模板），禁止笼统说"不好"
4. **Writer 在送审前必须先完成**：自查清单、通俗化复核、必要的题量验证
5. **用户拍板后标记 ✅**，才能进入下一轮
6. **中途打断不会乱**——每轮状态独立记录在下方，恢复时只需看最新一轮的状态
7. **工作记录只记摘要，不复制正文**，避免把 `ai_collab.md` 写成第二份任务文档
8. **强制同步检查点**——以下 4 个节点必须立即更新本文件的对应 Round 记录，不能事后补：
   - Writer 写完题目后 → 更新 `Writer 产出` + `Writer 自检` + `题量验证`，状态改为 `in_review`
   - Reviewer 审查完毕后 → 写入审查结论区，更新 `Reviewer 结论`
   - Writer 修完返修项后 → 追加 `修改记录`，状态改为 `in_review（N审）`
   - 用户拍板通过后 → 状态改为 `✅ done`，`Router 决议` 改为 `通过`
9. **快速恢复指引必须保持最新**——每次有新 Round 完成时，更新顶部「⚡ 快速恢复」区的进度描述和下一轮建议

### 一轮完成的标准

满足以下 5 条，才能把该轮标记为 `✅ done`：

1. 对应模块的 `README.md` 已完成本轮题目写入
2. Writer 已按 `AGENTS.md` 做完自查和通俗化复核
3. Reviewer 没有给出阻塞项，或阻塞项已全部关闭
4. 用户明确拍板通过

---

## 三、审查意见模板（GPT 用）

GPT 审查时请严格按此格式输出，方便 Antigravity 逐条处理：

```markdown
### 审查结论：✅ 通过 / ⚠️ 需修改 / ❌ 需重写

#### 必改问题
| # | 严重级别 | 题号 | 位置 | 检查项 | 具体问题 | 修改建议 |
|---|----------|------|------|--------|----------|----------|
| 1 | P0 / P1 / P2 | 第X题 | README 第X行 | 事实准确性 / 自查清单 / 通俗化 / 顺序约束 | 具体指出错在哪 | 给出可执行修改建议 |

#### 可选优化
| # | 题号 | 位置 | 优化点 | 建议 |
|---|------|------|--------|------|
| 1 | 第X题 | README 第X行 | 表达可更顺 | 建议怎么改 |

#### 结果摘要
- 是否存在阻塞项：有 / 无
- 是否允许进入下一轮：可以 / 不可以
- 若不可以：请明确说「先修完以上问题，再送审」
```

### 模板使用规则

1. **有问题就写问题，没有问题明确写"无阻塞项"**
2. **必须带位置**：至少写到文件 + 题号；能给行号更好
3. **优先写阻塞项**：事实错误、违背 `AGENTS.md`、顺序错位、导出未同步，这些优先级最高
4. **不要把检查表机械全打勾一遍**；重点列出不通过项和真正影响交付的点
5. **如果只是风格偏好，不要上升为必改**

---

## 四、工作记录

> 以下每一轮用 `### Round N` 标记，按时间顺序往下追加。  
> 完成的轮次折叠归档，只保留最新 2 轮展开。

---

### Round 模板

```markdown
### Round N - 模块名

- 状态：draft / in_review / revise / done
- 目标：本轮补哪几题，解决什么缺口
- 文件：`八股文面试题/模块/README.md`，`八股文面试题/导出/XX-模块.md`
- Writer 产出：新增/修改了哪些题
- Writer 自检：已完成 / 未完成
- Reviewer 结论：待审 / ✅ 通过 / ⚠️ 需修改 / ❌ 需重写
- Router 决议：待定 / 退回修改 / 通过进入下一轮
- 备注：只写恢复协作需要的信息，不贴长正文
```

<!-- 下面开始实际的工作记录 -->

### Round 1 - JVM

- 状态：✅ done
- 目标：补 2 道 P0 高频缺口题（GC 类型区分 + 对象晋升老年代条件）
- 文件：`八股文面试题/JVM/README.md`，`八股文面试题/导出/04-JVM.md`
- Writer 产出：新增 Q7（Minor/Major/Full GC 区别与触发条件）、Q8（对象晋升老年代的 4 种情况）；原 Q7-14 重编号为 Q9-16；复习优先级表已更新
- Writer 自检：已完成（7 项自查清单 + 8 项通俗化复核均通过）
- 题量验证：已完成。搜索「JVM 面试必问 高频题 2025 javaguide」，16 题覆盖 JVM 内存区域、堆栈区别、对象创建、GC Roots/可达性分析、GC 算法、CMS vs G1、GC 类型、对象晋升、类加载、双亲委派、引用类型、调优参数、OOM 排查、内存泄漏、对象头、直接内存。建议 12-15 题，当前 16 题达标
- 一审修改记录：按 GPT 审查修改 3 处（HotSpot 限定语、G1 Humongous Region、题量验证说明）
- Reviewer 结论：✅ 通过（二审确认一审 3 个必改项全部关闭）
- Router 决议：通过，进入下一轮

---

### Round 2 - MySQL

- 状态：✅ done
- 目标：补 2 道 P0 高频缺口题（Buffer Pool 缓冲池 + 两阶段提交）
- 文件：`八股文面试题/MySQL/README.md`，`八股文面试题/导出/06-MySQL.md`
- Writer 产出：新增 Q10（Buffer Pool 缓冲池，含脏页/LRU/Flush 链表/预读机制 + 参数配置追问）、Q11（两阶段提交，含 prepare→binlog→commit 流程 + 崩溃恢复判断逻辑 + 组提交优化追问）；原 Q10-15 重编号为 Q12-17；复习优先级表已更新
- Writer 自检：已完成（含 web search 题量验证）
- 题量验证：已完成。搜索「MySQL 面试必问 高频题 2025 javaguide」，17 题已覆盖所有高频考点（存储引擎/B+树/聚簇索引/索引失效/ACID/隔离级别/MVCC/锁/三大日志/Buffer Pool/两阶段提交/Explain/慢SQL/最左前缀/分库分表/主从复制/Binlog格式）。搜索中额外提到的「SQL 执行过程」和「索引下推 ICP」属中低频，本轮不补
- 一审修改记录：修改 5 处——① Q10 LRU 首次出现补「最近最少使用」中文 ② Q10 脏页恢复改为「重放已提交但未刷盘的修改」 ③ Q11 崩溃恢复判断改为「在 Binlog 中找到完整 XID」+ 话术补「Binlog 是事实来源」 ④ Q11 刷盘两次补高可靠配置前提 ⑤ 本记录补 web search 验证
- Reviewer 结论：✅ 通过（三审确认二审遗留的 Q11 表头问题已关闭）
- Router 决议：通过，进入下一轮

### 审查结论：⚠️ 需修改

#### 必改问题
| # | 严重级别 | 题号 | 位置 | 检查项 | 具体问题 | 修改建议 |
|---|----------|------|------|--------|----------|----------|
| 1 | P0 | Round 2 记录 | `ai_collab.md` 第 134 行；`AGENTS.md` 第 218-227 行 | 自查清单 / 题量充分性验证 | Round 2 记录写的是“整章验证留到收口时执行”，但 `AGENTS.md` 已把题量充分性验证列为通俗化复核后的**强制后置步骤**，`ai_collab.md` 也要求 Writer 在送审前完成必要的题量验证。当前 `Writer 自检` 仍写“已完成”，流程上不成立。 | 送审前补做 MySQL 章节的 web search 高频题验证，并把验证结果写回本轮记录；如果暂时不做，至少要把 `Writer 自检` 改成“未完成”，不能按“已完成”送审。 |
| 2 | P1 | 第 11 题 | `八股文面试题/MySQL/README.md` 第 246-256 行 | 事实准确性 / 具体到实现 | “Binlog 是否完整”这个判断条件写得太粗，容易让读者误以为崩溃恢复时只是看 binlog 文件有没有写完。MySQL 恢复时实际会扫描最后一个 binlog，收集**完整写入 binlog 的事务 XID**，再让 InnoDB 对 `prepare` 状态的事务按 XID 决定提交还是回滚。 | 把表格和面试话术里的“Binlog 是否完整”改成“能否在 binlog 中找到该事务对应的完整 XID / 事务事件”，顺手补一句“binlog 是恢复阶段的事实来源”。 |
| 3 | P2 | 第 10 题 | `八股文面试题/MySQL/README.md` 第 208-214 行 | 通俗化 | `LRU` 首次出现时没有中文解释，和 `AGENTS.md` 通俗化复核第 2 条“英文缩写加中文”不一致。对基础薄弱的读者，这个缩写不够直观。 | 首次出现改成“最近最少使用（LRU）链表”或“按最近访问时间淘汰的 LRU 链表”，后文再直接写 `LRU`。 |

#### 可选优化
| # | 题号 | 位置 | 优化点 | 建议 |
|---|------|------|--------|------|
| 1 | 第 10 题 | `八股文面试题/MySQL/README.md` 第 207 行 | 表述可更准 | “崩溃后通过 Redo Log 恢复脏页数据”可改成“崩溃后通过 Redo Log 重放已提交但未刷盘的修改”，避免把“脏页”这个内存概念和磁盘恢复混在一起。 |
| 2 | 第 11 题 | `八股文面试题/MySQL/README.md` 第 258 行 | 条件可更严谨 | “每次事务提交都要刷盘两次”前面可补“在常见高可靠配置下（如 `innodb_flush_log_at_trx_commit=1` 且 `sync_binlog=1`）”，避免写成绝对表述。 |

#### 结果摘要
- 是否存在阻塞项：有
- 是否允许进入下一轮：不可以
- 若不可以：先修完以上问题，再送审

### 二审结论：⚠️ 仍需修改

#### 必改问题
| # | 严重级别 | 题号 | 位置 | 检查项 | 具体问题 | 修改建议 |
|---|----------|------|------|--------|----------|----------|
| 1 | P1 | 第 11 题 | `八股文面试题/MySQL/README.md` 第 248 行 | 事实准确性 / 具体到实现 | 一审要求把“Binlog 是否完整”改成更准确的“能否在 Binlog 中找到该事务对应的完整 XID / 事务事件”。当前第 250-256 行的行内描述和话术已经改对了，但表格表头第 248 行仍保留旧表述 `Binlog 是否完整`，会把恢复判断再次说粗。 | 把表头改成“Binlog 中能否找到该事务对应的完整 XID / 事务事件”或同等精度的说法，和表格内容、面试话术保持一致。 |

#### 可选优化
| # | 题号 | 位置 | 优化点 | 建议 |
|---|------|------|--------|------|
| 1 | 第 10 题 | `八股文面试题/MySQL/README.md` 第 207-210 行 | 已关闭 | 一审 2 个可选优化中的 Q10 表述已修正，可保持现状。 |
| 2 | 第 11 题 | `八股文面试题/MySQL/README.md` 第 258 行 | 已关闭 | “刷盘两次”已补高可靠配置前提，可保持现状。 |

#### 结果摘要
- 必改项关闭情况：3 项中已关闭 2 项，1 项未关闭
- 可选优化关闭情况：2 项均已关闭
- 是否存在阻塞项：有
- 是否允许进入下一轮：不可以
- 若不可以：先把 Q11 表头这 1 处修完，再送审

### 三审结论：✅ 通过

#### 必改问题
无阻塞项。二审遗留的最后 1 个必改项已关闭：`八股文面试题/MySQL/README.md` 第 248 行已从旧表述 `Binlog 是否完整` 改为 **`Binlog 中能否找到该事务的完整 XID`**，与第 250-256 行的恢复决策和面试话术保持一致。

#### 可选优化
无新增优化项。前两轮提到的 2 个可选优化保持关闭状态。

#### 结果摘要
- 是否存在阻塞项：无
- 是否允许进入下一轮：可以

---

### Round 3 - 计算机网络

- 状态：✅ done
- 目标：补 1 道 P0 高频缺口题（HTTP 版本演进）
- 文件：`八股文面试题/计算机网络/README.md`，`八股文面试题/导出/08-计算机网络.md`
- Writer 产出：新增 Q6（HTTP/1.0~3.0 版本演进，含持久连接/队头阻塞/多路复用/QUIC）；原 Q6-10 重编号为 Q7-11，共 11 题
- Writer 自检：已完成
- 题量验证：已完成。搜索「计算机网络 面试必问 高频题 2025 javaguide」，11 题覆盖 TCP/IP 模型、TCP vs UDP、三次握手、四次挥手、HTTP vs HTTPS、HTTP 版本演进、状态码、GET vs POST、URL 全过程、Cookie/Session、TCP 可靠传输。建议 8-12 题，当前 11 题达标
- 一审修改记录：HTTP/1.0 表述从绝对化改为"默认短连接"
- Reviewer 结论：✅ 通过（二审确认本轮相关问题已关闭）
- Router 决议：通过

### 二审结论：✅ 通过

#### 必改问题
无阻塞项。本轮相关的 2 个必改项已关闭：
1. 原先把 3 个模块混写在同一轮的问题，现已拆分为独立的 `Round 3/4/5`，当前 `Round 3` 只处理**计算机网络**模块。
2. 题量验证已改为按技术栈单独记录，`Round 3` 已单独写明「计算机网络 面试必问 高频题 2025 javaguide」的验证结果。

#### 可选优化
已关闭。`八股文面试题/计算机网络/README.md` 第 93 行已改为“**默认短连接**，通常一次请求一个 TCP 连接”，不再是绝对化表述。

#### 结果摘要
- 必改项关闭情况：本轮相关 2 项已关闭
- 可选优化关闭情况：1 项已关闭
- 是否存在阻塞项：无
- 是否允许进入下一轮：可以

---

### Round 4 - 操作系统

- 状态：✅ done
- 目标：补 1 道 P0 高频缺口题（IO 模型 BIO/NIO/AIO）
- 文件：`八股文面试题/操作系统/README.md`，`八股文面试题/导出/09-操作系统.md`
- Writer 产出：新增 Q9（IO 模型 BIO/NIO/AIO/多路复用，含 select/poll/epoll 对比 + Java IO 映射 + Netty/Redis 关联）；共 9 题
- Writer 自检：已完成
- 题量验证：已完成。搜索「操作系统 面试必问 高频题 2025 javaguide」，9 题覆盖进程线程、IPC、进程状态、调度算法、死锁、虚拟内存、内存布局、用户态内核态、IO 模型。建议 6-10 题，当前 9 题达标
- 一审修改记录：① OS 同步非阻塞与 Java NIO 概念区分，加明确标注和区分说明 ② select 1024 补"Linux 常见默认实现"限定
- Reviewer 结论：✅ 通过（二审确认 Q9 的 1 个必改项和 1 个可选优化均已关闭）
- Router 决议：通过

### 二审结论：✅ 通过

#### 必改问题
无阻塞项。`八股文面试题/操作系统/README.md` 第 143 行已改成“**同步非阻塞 IO（注意：不是 Java NIO 包）**”，第 165 行也补了概念区分说明，已经把**操作系统同步非阻塞 IO**和**Java `java.nio` 多路复用**拆开讲清楚。

#### 可选优化
已关闭。`select` 的“1024 个文件描述符上限”已在第 151 行补上“**在 Linux 常见默认实现中**”这个限定语。

#### 结果摘要
- 必改项关闭情况：1 项已关闭
- 可选优化关闭情况：1 项已关闭
- 是否存在阻塞项：无
- 是否允许进入下一轮：可以

---

### Round 5 - 消息队列

- 状态：✅ done
- 目标：补 1 道 P0 高频缺口题（Kafka 核心架构）
- 文件：`八股文面试题/消息队列/README.md`，`八股文面试题/导出/12-消息队列.md`
- Writer 产出：新增 Q2（Kafka 核心架构，含 Topic/Partition/Consumer Group/Offset/Replica/Leader/Follower/ISR + 高吞吐 5 大原因）；原 Q2 MQ 选型降为 Q3，原 Q3-7 重编号为 Q4-8，共 8 题
- Writer 自检：已完成
- 题量验证：已完成。搜索「消息队列 面试必问 高频题 2025 javaguide」，8 题覆盖 MQ 作用、Kafka 架构、MQ 选型、消息不丢失、幂等性、顺序性、死信队列、消息积压。建议 7-10 题，当前 8 题达标
- 一审修改记录：① Kafka 架构从 Q3 提前到 Q2（MQ 选型降为 Q3），符合 AGENTS.md「先介绍再对比」顺序约束 ②概念表补 Replica/Leader/Follower 定义后再讲 ISR，消除概念断层
- Reviewer 结论：✅ 通过（二审确认 Q2 的顺序和概念断层问题已关闭）
- Router 决议：通过

### 二审结论：✅ 通过

#### 必改问题
无阻塞项。`八股文面试题/消息队列/README.md` 已把 **Kafka 核心架构** 提前到 `Q2`，`MQ 选型` 顺延到 `Q3`，顺序已符合 `AGENTS.md` 的“先介绍，再对比”要求；同时概念表已先补 `Replica / Leader / Follower`，再讲 `ISR`，原先的概念断层已消除。

#### 可选优化
本轮无可选优化遗留项。

#### 结果摘要
- 必改项关闭情况：1 项已关闭
- 可选优化关闭情况：本轮无
- 是否存在阻塞项：无
- 是否允许进入下一轮：可以

### 审查结论：⚠️ 需修改

#### 必改问题
| # | 严重级别 | 题号 | 位置 | 检查项 | 具体问题 | 修改建议 |
|---|----------|------|------|--------|----------|----------|
| 1 | P0 | Round 3 记录 | `ai_collab.md` 第 193-201 行；`ai_collab.md` 第 34-39 行 | 协作流程 | `Round 3` 把**计算机网络 / 操作系统 / 消息队列** 3 个模块打包到同一轮，直接违反了协作协议里“**一轮只处理一个模块**”的固定规则。这样会把不同模块的审查、返修、通过状态绑在一起，后续恢复协作也会混乱。 | 把当前记录拆成 3 个独立轮次，或至少拆成 3 段独立的 Round 记录，分别记录目标、产出、审查结论和 Router 决议，不能继续以“一个 Round 审 3 个模块”的形式推进。 |
| 2 | P0 | Round 3 记录 | `ai_collab.md` 第 202-203 行；`AGENTS.md` 第 218-227 行 | 题量充分性验证 | `Writer 自检` 写“含 web search 题量验证”，但实际记录只写了一个合并查询 `计算机网络 操作系统 消息队列 Kafka 面试高频题 2025`。`AGENTS.md` 的强制后置流程要求按**技术栈名逐个搜索**，并对比高频题差异；当前写法既不是逐模块验证，也没有列出每个模块的差异表。 | 分别补做 3 次验证：`计算机网络 面试必问 高频题 2025 javaguide`、`操作系统 面试必问 高频题 2025 javaguide`、`消息队列 面试必问 高频题 2025 javaguide`，并把每个模块的“已覆盖 / 缺失 / 是否需要补题”单独写回记录区。 |
| 3 | P1 | 第 9 题 | `八股文面试题/操作系统/README.md` 第 136-167 行 | 区分场景 / 通俗化 | 这题把**操作系统里的“同步非阻塞 IO”**和**Java 里的 `NIO`** 混在了一起。前面表格把 `NIO` 写成“轮询检查”，后面 Java API 映射又把 `java.nio` 对应成“IO 多路复用”，口诀也写成“NIO 一线程多连接靠多路复用”。对基础薄弱的读者来说，这会变成两个不同概念共用一个名字。 | 把第 143 行这一行改成“**同步非阻塞 IO（不是 Java NIO 包）**”，并在后面单独补一句：“Java 面试里常说的 `NIO` 多指 `java.nio` + `Selector` 的多路复用方案，不等于单纯轮询检查。” 让 OS 模型和 Java API 两层概念分开。 |
| 4 | P1 | 第 3 题 | `八股文面试题/消息队列/README.md` 第 21-35 行；`AGENTS.md` 第 128-138 行 | 顺序约束 / 内部引用 | `AGENTS.md` 明确写了“**先分别介绍 Kafka、RabbitMQ → 再做对比**”。现在文件是 Q2 先做 MQ 选型对比，Q3 才开始讲 Kafka 架构，顺序反了。并且 Q3 里的 `ISR` 行直接引用了 `Leader`、`Replica`，但前面没有先解释这两个概念，读者会断层。 | 至少把 `Kafka 核心架构` 挪到选型对比题前面，或者补一题 RabbitMQ / RocketMQ 基础介绍后，再保留选型对比题；同时在 Q3 的概念表里先补 `Replica`、`Leader`、`Follower` 的定义，再讲 `ISR`。 |

#### 可选优化
| # | 题号 | 位置 | 优化点 | 建议 |
|---|------|------|--------|------|
| 1 | 第 6 题 | `八股文面试题/计算机网络/README.md` 第 93 行 | 表述可更准 | `HTTP/1.0` 那行的“每次请求都要新建 TCP 连接”可以收一下，改成“**默认是短连接**，通常一次请求一个 TCP 连接”，避免写成绝对表述。 |
| 2 | 第 9 题 | `八股文面试题/操作系统/README.md` 第 151-153 行 | 实现限定可更清楚 | `select` 的“1024 个文件描述符上限”建议补一句“**在 Linux 常见默认实现里**”，避免把实现细节说成所有系统都一样。 |

#### 结果摘要
- 是否存在阻塞项：有
- 是否允许进入下一轮：不可以
- 若不可以：先修完以上问题，再送审

---

### Round 6 - 计算机网络

- 状态：✅ done
- 目标：补 1 道 P1 中频缺口题（HTTPS TLS 握手过程）
- 文件：`八股文面试题/计算机网络/README.md`
- Writer 产出：新增 Q6（HTTPS TLS 握手过程，含 TLS 1.2 RSA 密钥交换全流程图 + (EC)DHE 主流方案说明、对称/非对称加密角色分工、三个随机数生成会话密钥、证书验证 4 步、TLS 1.3 优化与前向保密）；原 Q6-11 重编号为 Q7-12，共 12 题；复习优先级表已更新，Q6 归入 P1
- Writer 自检：已完成（7 项自查清单 + 8 项通俗化复核均通过）
- 题量验证：已完成。搜索「计算机网络 面试必问 高频题 2025 javaguide」，12 题覆盖 TCP/IP 模型、TCP vs UDP、三次握手、四次挥手、HTTP vs HTTPS、TLS 握手、HTTP 版本演进、状态码、GET vs POST、URL 全过程、Cookie/Session、TCP 可靠传输。建议 8-12 题，当前 12 题达标
- 一审修改记录：修改 4 处——① 标题和流程图加 RSA 场景限定，补 (EC)DHE 主流方案说明注释框 ② TLS 1.3 段修正：不再支持 RSA 和静态 DH → 首次握手主流 ECDHE，恢复连接基于 PSK，0-RTT 仅限恢复连接且有重放风险 ③ 所有英文缩写首次出现补中文释义（传输层安全协议 TLS、证书颁发机构 CA、在线证书状态协议 OCSP、证书吊销列表 CRL、来回通信 RTT、椭圆曲线迪菲-赫尔曼临时密钥交换 ECDHE），Change Cipher Spec/Finished 改为中文「切换加密通知」「完成验证」 ④ 证书吊销补"具体是否在线校验取决于客户端/浏览器策略"；面试话术同步修改为区分 TLS 1.2 RSA 和 TLS 1.3 ECDHE
- Reviewer 结论：✅ 通过（二审确认一审 3 个必改项全部关闭）
- Router 决议：通过，进入下一轮
- 备注：P1 第 1 题完成，下一轮 Round 7 建议补 P1 第 2 题（线程池核心线程数，Java 并发）

---

### Round 7 - Java 并发

- 状态：✅ done
- 目标：补 P1 高频缺口（线程池核心线程数怎么设置），按 GPT 建议并入 Q12 线程池参数作为追问补强
- 文件：`八股文面试题/Java并发/README.md`
- Writer 产出：在 Q12（线程池 7 大参数）末尾追加「高频追问：核心线程数怎么设置」。内容包括：CPU 密集型（核数/核数+1）与输入输出（IO）密集型（核数×2）经验起点值 + 精确公式（核数×(1+等待/计算)）+ 4核80ms/20ms→20线程具体例子 + 面试挖坑提醒（无界队列陷阱、下游瓶颈、混合任务拆池） + 背诵口诀 + 面试话术。题量不变仍为 17 题
- Writer 自检：已完成（7 项自查清单 + 8 项通俗化复核均通过）
- 题量验证：已完成。搜索「Java并发 面试必问 高频题 2025 javaguide」，17 题覆盖所有主流高频考点。建议 15-20 题，当前 17 题达标
- 一审修改记录：修改 4 处——① IO 首次出现补中文释义「输入输出（IO）密集型」② 补背诵口诀「CPU 看核数，IO 看等待，先估后测再定值」③ 公式后补人话解释「等 IO 4 倍于 CPU 计算，线程数开到核数 5 倍」④「内存页缺失等偶发阻塞」改口语化为「线程偶尔被系统短暂卡一下」
- Reviewer 结论：✅ 通过（二审确认一审 2 个必改项全部关闭）
- Router 决议：通过，进入下一轮
- 备注：P1 第 2 题完成，下一轮 Round 8 建议补 P1 第 3 题（零拷贝，操作系统）

---

### Round 8 - 操作系统

- 状态：✅ done
- 目标：补 1 道 P1 中频缺口题（零拷贝 Zero Copy）
- 文件：`八股文面试题/操作系统/README.md`
- Writer 产出：新增 Q10（零拷贝，含传统 IO 4拷4切换流程图、mmap vs sendfile 对比表、SG-DMA 进化、Java API 映射 FileChannel.transferTo/MappedByteBuffer、Kafka/RocketMQ 应用场景、Kafka 为什么快 5 点追问、背诵口诀、面试话术）；复习优先级表已更新，Q10 归入 P1；共 10 题
- Writer 自检：已完成（7 项自查清单 + 8 项通俗化复核均通过）
- 题量验证：已完成。搜索「操作系统 面试必问 高频题 2025 javaguide」，10 题覆盖所有主流高频考点。建议 6-10 题，当前 10 题达标
- 一审修改记录：修改 6 处——① DMA 首次出现补中文「直接内存访问（DMA）」② mmap 适用场景拆分为「典型场景」+「产品示例」，sendfile 角色改为「Kafka Broker 向消费者发送消息」③ Java API 表标题加「以 Linux 常见实现为例」，transferTo 改为「通常利用 sendfile 能力，走不通时会回退」④ Page Cache 补中文「页缓存（Page Cache）」⑤ 口诀修正为「传统 4 拷 4 切换，mmap 省一拷，sendfile 再省一拷；有 SG-DMA 时 CPU 拷贝归零」⑥ 面试话术同步修改
- Reviewer 结论：✅ 通过（二审确认一审 4 个必改项全部关闭）
- Router 决议：通过，进入下一轮
- 备注：P1 第 3 题完成，下一轮 Round 9 建议补 P1 第 4 题（联合索引底层 B+ 树，MySQL）

---

### Round 9 - MySQL

- 状态：✅ done
- 目标：补强 P1 高频缺口（联合索引底层 B+ 树），并入 Q14 最左前缀原则作为追问补强
- 文件：`八股文面试题/MySQL/README.md`
- Writer 产出：大幅补强 Q14（最左前缀原则）：加联合索引在 B+ 树中的存储图示（(Alice,20)→(Bob,18) 具体数据）、排序原理解释、范围查询中断匹配规则、MySQL 优化器自动调序说明、索引下推 ICP 对比表（MySQL 5.6）、背诵口诀、面试话术。题量不变仍为 17 题
- Writer 自检：已完成（7 项自查清单 + 8 项通俗化复核均通过）
- 题量验证：已完成。搜索「MySQL 面试必问 高频题 2025 javaguide」，17 题覆盖 InnoDB vs MyISAM / B+ 树索引 / 聚簇索引回表 / 索引失效 / ACID / 隔离级别 / MVCC / 锁 / 三大日志 / Buffer Pool / 两阶段提交 / Explain / 慢 SQL 优化 / 最左前缀+联合索引底层+ICP / 分库分表 / 主从复制 / Binlog 格式。本轮补强后"联合索引底层 B+ 树"缺口已关闭，无高频/中频新增缺失。建议 15-20 题，当前 17 题达标
- 一审修改记录：修改 3 处——① ICP 示例补完整 SQL（SELECT * FROM user WHERE ...）+ 前提说明「因为是 SELECT *，需要读取非索引列，所以会回表」+ 表格「回表次数」改为「读取完整行次数」② 最左匹配表 WHERE b = 2 加 Skip Scan（MySQL 8.0.13+）限定语，范围查询行加「ICP 可能在索引层过滤 c」③ 题量验证补完整搜索词和考点对比记录
- Reviewer 结论：✅ 通过（二审确认一审 2 个必改项全部关闭）
- Router 决议：通过，进入下一轮
- 备注：P1 第 4 题完成，下一轮 Round 10 建议补 P1 第 5 题（RocketMQ 事务消息，消息队列）

---

### Round 10 - 消息队列

- 状态：✅ done
- 目标：补 1 道 P1 中频缺口题（RocketMQ 事务消息）
- 文件：`八股文面试题/消息队列/README.md`
- Writer 产出：新增 Q9（RocketMQ 事务消息，含电商下单场景引入、半消息+两阶段提交+事务回查流程图、关键概念表、最终一致性挖坑、事务消息 vs 本地消息表对比、背诵口诀、面试话术）；复习优先级表已更新，Q9 归入 P1；共 9 题
- Writer 自检：已完成（7 项自查清单 + 8 项通俗化复核均通过）
- 题量验证：已完成。搜索「消息队列 面试必问 高频题 2025 javaguide」，9 题覆盖为什么用 MQ / Kafka 架构 / MQ 选型 / 消息不丢失 / 幂等性 / 顺序性 / 死信队列 / 消息积压 / RocketMQ 事务消息。本轮补上后"事务消息"缺口已关闭。建议 7-10 题，当前 9 题达标
- 一审修改记录：修改 4 处——① 流程图第③步加 UNKNOW 状态、第④步触发条件改为「返回 UNKNOW 或丢失时」② 关键概念表二次确认加 UNKNOW、事务回查删「默认最多 15 次」改为「按间隔多次回查，超过上限或超时后默认回滚」③ 挖坑提醒加「详见本章第 4 题和第 5 题」④ 面试话术 Commit/Rollback 补中文「提交/回滚」+ 补 UNKNOW + 删绝对回查次数
- Reviewer 结论：✅ 通过（二审确认一审 2 个必改项全部关闭）
- Router 决议：通过
- 备注：**🎉 P1 全部 5 个缺口已补完！**后续可进入 P2 阶段

---

### Round 11 - 分布式与微服务

- 状态：✅ done
- 目标：补 4 道实战组件题（限流算法、Seata AT 模式、TCC 模式 + 空回滚/幂等/悬挂、接口幂等性），将章节从 15 题踩线提升至 19 题。题目内容属 P1 中频，由 P2 阶段工作流触发
- 文件：`八股文面试题/分布式与微服务/README.md`
- Writer 产出：新增 Q16（Sentinel 限流算法，含固定窗口/滑动窗口/漏桶/令牌桶 4 算法对比 + Sentinel 双算法实现）、Q17（Seata AT 模式，含 TC/TM/RM 三大角色 + 两阶段流程图 + undo_log + 与 2PC 对比）、Q18（TCC 模式，含三阶段转账举例 + AT vs TCC 对比 + 空回滚/幂等/悬挂三大经典坑解决方案）、Q19（接口幂等性，含 5 种方案对比表 + Token 令牌机制完整流程 + HTTP 幂等追问）；复习优先级表已更新，Q16-19 归入 P1；共 19 题
- Writer 自检：已完成（7 项自查清单 + 8 项通俗化复核均通过）
- 题量验证：已完成。搜索「分布式与微服务 面试必问 高频题 2025 javaguide Nacos Sentinel Seata TCC」，19 题覆盖 CAP/BASE/分布式事务/分布式 ID/微服务组件/熔断降级/Nacos/Sentinel/Gateway/OpenFeign/Nacos 配置中心/限流算法/Seata AT/TCC/接口幂等/负载均衡/调用链路/Spring Cloud vs Dubbo/RPC vs HTTP。建议 15-18 题，当前 19 题超出上限，高频中频考点全覆盖
- 一审修改记录：修改 4 处——① Q16 Sentinel 匀速排队从「令牌桶」改为「漏桶」（一句话总结/实现说明/口诀/话术/挖坑提醒 5 处统一修正）②  Q17 Seata AT 全局锁机制补全：一阶段提交前须先获取全局锁、释放数据库本地锁但全局锁持有到事务结束（一句话总结/流程图/2PC 对比 3 处修正）③ Q19 OpenFeign 重试默认值从「默认超时重试」改为「Feign 原生默认重试 IO 异常，Spring Cloud OpenFeign 默认关闭重试」④ Q19 HTTP 幂等方法从「GET 和 DELETE 天然幂等」改为「GET/HEAD/PUT/DELETE 语义幂等，POST 通常不是」+ 补「HTTP 语义幂等 ≠ 业务幂等」
- 二审修改记录：修改 1 处——Q19 Token 令牌机制表格中「Redis SETNX」改为「Redis DEL」，与正文 DEL 实现保持一致
- Reviewer 结论：✅ 通过（三审确认二审遗留的 Q19 SETNX→DEL 已关闭）
- Router 决议：通过
- 备注：分布式与微服务从 15 题提升至 19 题，超出建议上限 18 题，高频中频考点全覆盖

